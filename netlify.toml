[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — semua route ke index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
